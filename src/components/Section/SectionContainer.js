import SectionBar from "./SectionBar.js"

/*
@props: {
    className: "todo-bar| completed-bar";
    sectionTitle: string;
}
*/
export default function SectionContainer(props) {
 
    return (
    <div>
        <SectionBar className="props.className"/>
    </div>
    )
}